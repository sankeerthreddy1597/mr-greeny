## IDF Component Manager Manifest File
dependencies:
  waveshare/esp32_s3_touch_amoled_1_75:
    version: "*"
  lvgl/lvgl:
    version: "9.4.*"
    public: true
  espressif/esp_websocket_client:
    # Pinned below 1.6.0: that release added esp_transport_ws_get_redir_uri,
    # which needs a newer tcp_transport than what ships in ESP-IDF v5.5.0.
    version: "~1.5"
